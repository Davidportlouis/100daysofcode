#include<stdio.h>
#include<stdlib.h>

int main(void)
{
    int start,end,i,flag,n,tc=0;
    while(tc < n)
    {
        scanf("%d %d",&start,&end);
        while(start < end)
        {
            flag = 0;
            
        }
        tc++;
    }
}